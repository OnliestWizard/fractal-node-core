fractal-node-core

This project explores execution-first, language-agnostic logic nodes.

Nodes define intent, inputs, outputs, and side-effects.
Emitters translate nodes into platform-specific execution targets.

This repository intentionally avoids:
- syntax parsing
- full compilers
- visual tooling
- AI generation

The goal is to prove that intent can survive translation.
