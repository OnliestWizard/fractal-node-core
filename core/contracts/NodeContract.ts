// NodeContract is derived from NodeDefinition — the portable, runtime-free subset.
// Import from here for contract-layer consumers (emitters, registries, serialisers).
export type { NodeContract, Port as NodePort, ValueType as PortType, SideEffect } from '../types'
